// tests/fixtures/fake-callback.ts

import { headers as giteeHeaders, payload as giteePayload } from './gitee'


// this will create a fake callback to the webhook listener with specific payload
export function getFakeCallback(provider: string, port: number): any {
  
}
